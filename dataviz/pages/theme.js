import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    primary: { main: "#124469" }, // Inato's logo color.
    secondary: { main: "#b3915f" } // Complementary according to Adobe palette.
  }
});

export default theme;
